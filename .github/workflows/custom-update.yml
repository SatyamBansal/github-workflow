name: Test action
on:
  issue_comment:
    types: [created]
jobs:
  verion-update:
    name: Taaza tazza action
    runs-on: ubuntu-latest
    steps:
      - name: Checking out code
        run: |
          echo "I am first"
      - uses: actions/checkout@v3
      - name: Listing pwd
        run: |
          ls
      - name: running script
        run: |
        chmod +x updateVersion.sh
        git add iosVersion.txt
        git -c user.name="bot" user.email="bot@github.com" commit -m "updated ios version"
        git push origin 
